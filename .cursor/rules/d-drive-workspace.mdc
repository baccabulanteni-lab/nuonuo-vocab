---
description: 项目与工作目录一律使用 D 盘，避免写入 C 盘
alwaysApply: true
---

# 工作目录与存储位置（D 盘优先）

- **项目根目录**：新建/克隆/解压的代码仓库一律放在 **`D:\` 下**（例如 `D:\糯糯专升本英语`、`D:\糯糯背单词`），不要在 `C:\Users\...\Documents` 或桌面（常在 C 盘）长期放源码。
- **终端命令**：执行 `git clone`、`npm create`、`npx`、解压工具前，先 `D:` 并 `cd` 到 `D:\...` 目标路径再操作。
- **构建产物**：`dist`、`build`、`node_modules` 允许在项目目录内生成；**不要将整仓或大量依赖复制到 C 盘**。
- **下载/导出**：安装包、静态资源、数据库导出等优先保存到 `D:\` 下的固定目录（如 `D:\downloads-dev`）。
- **说明**：Cursor/编辑器**自身配置**可能仍在系统盘，此规则针对**你与 AI 协作的代码与资源**路径；新建文件、脚本路径、文档中的路径示例默认用 `D:\`。
